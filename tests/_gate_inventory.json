{
 "_why": "Every error site in a load-bearing bdbot module, from the AST. `covered` has a case in verify/verify_gates_actually_fire.py; `known_unobserved` has never been seen to fire and nobody has written one yet. A site in neither fails the suite -- that is the whole mechanism.",
 "covered": [
  "health:raise RuntimeError(f'[NUM_DIVERGE] step {timestep}: |r|max={far:.3g} > 50\u00b7L={50 * self.box_L:.3g}')",
  "health:raise RuntimeError(f'[NUM_NONFINITE] step {timestep}: PE={pe}')",
  "health:raise RuntimeError(f'[NUM_NONFINITE] step {timestep}: {n} non-finite position(s)')",
  "health:raise ValueError(f'D_expected must be > 0, got {D_expected}')",
  "health:raise ValueError(f'need >= 4 points for an FDT residual, got {msd.size}')",
  "metrics:raise ValueError(f\"[{name}] composite + implementation_check requires a derivation (rule 7). State why that analytic expression is also derivable for the combin",
  "metrics:raise ValueError(f'role must be one of {ROLES} (got: {role!r})')",
  "nondim:raise ValueError(f'schema differs: {got!r} (expected {SCHEMA!r})')",
  "params:raise ValueError(f'category must be one of {CATEGORIES}, got {category!r}')",
  "params:raise ValueError(f'declaring {category!r} not applicable requires a reason -- otherwise it is indistinguishable from forgetting it (rule 3)')",
  "params:raise ValueError(f'{name!r}: say who or what would supply it')",
  "scales:raise ValueError(f\"emptying role '{role}' requires a reason (rule 3)\")"
 ],
 "known_unobserved": [
  "design:raise ValueError(f\"abandoning '{self.symbol}' requires `safe_because`: why is it safe to give this group up in THIS system? A reason says what you did; safe_bec",
  "design:raise ValueError(f\"abandoning '{self.symbol}' requires a reason -- same rule as scales.declare_absent (rule 3)\")",
  "goal:Issue('error', 'analysis_implied', 'empty. This is the field the analysis stage reads to decide what to plot; with nothing here the goal cannot reach the end of",
  "goal:Issue('error', 'analysis_implied', 'must be a list')",
  "goal:Issue('error', 'answering_quantity', 'must be a mapping')",
  "goal:Issue('error', 'answering_quantity.symbol', 'empty. The analysis stage matches this against a measured observable name; without it the goal cannot be checked ag",
  "goal:Issue('error', 'decisive_precision', 'must be a mapping')",
  "goal:Issue('error', 'decisive_precision', 'value is null and basis is empty. A null value is allowed -- often it is unknown until the design exists -- but then basis",
  "goal:Issue('error', 'decisive_precision.what_would_change_my_mind', \"empty. A question nothing could refute is not a question -- this is S1's falsifiability gate.\")",
  "goal:Issue('error', 'physics_that_matters', 'empty. The point of stating a goal first is that it tells you which parameters matter.')",
  "goal:Issue('error', 'physics_that_matters', 'must be a list')",
  "goal:Issue('error', 'provenance', 'must be a mapping')",
  "goal:Issue('error', 'provenance.source', 'empty. Where did this goal come from -- a conversation, a paper, the sketch? (rule 3)')",
  "goal:Issue('error', 'question', 'empty. This is the field the whole pipeline exists to answer; if it is unknown, ask rather than guess (rule 3).')",
  "goal:Issue('error', 'question', f'too short ({len(q)} chars) to be a question -- that is a label. Say what would be learned.')",
  "goal:Issue('error', at, 'must be a mapping {symbol, why}')",
  "goal:Issue('error', f\"{at} {it.get('symbol', '?')}\", f\"'{need}' is empty\")",
  "goal:Issue('error', k, 'required field missing')",
  "goal:Issue('error', str(p.name), \"goal.yaml is missing. Create it with `bdbot goal init <folder>`. The sketch may legitimately state no goal (rule 5) -- this file is",
  "health:raise AssertionError(f'{name}: relative std {rel:.3e} < {min_rel_std:.3e} -- the statistic does not fluctuate. It is probably an arithmetic identity, not a meas",
  "health:raise RuntimeError(f'[NUM_DIVERGE] step {timestep}: PE {pe:.3g} is {abs(pe) / self.pe0:.0f}x the initial {self.pe0:.3g} (limit {self.pe_blowup:.0f}x)')",
  "health:raise ValueError('need >= 2 samples')",
  "health:raise ValueError('need >= 4 matching (t, msd) points')",
  "health:raise ValueError('need >= 4 strictly positive (t, msd) points for a log-log slope')",
  "health:raise ValueError('t must be > 0')",
  "health:raise ValueError(f'laplacian_U_total must be > 0, got {laplacian_U_total}')",
  "health:raise ValueError(f'msd and t must be 1-D and the same length, got {msd.shape} and {t.shape}')",
  "intake:Issue('error', 'raw_transcription', 'the transcription is empty or too short. Rule 5: transcribe before you interpret.')",
  "intake:Issue('error', at, 'must be a mapping')",
  "intake:Issue('error', at, 'source is empty (rule 3: every number carries a source).')",
  "intake:Issue('error', at, 'there is an assumed_value but no confidence (tier). A value with no source does not go in (rule 3).')",
  "intake:Issue('error', at, f\"duplicate id: {it['id']}\")",
  "intake:Issue('error', at, f\"required key '{r}' missing (present in 100% of the five real files)\")",
  "intake:Issue('error', at, f'kind must be one of {MISSING_KINDS}.')",
  "intake:Issue('error', k, f'required field missing. {hint}'.strip())",
  "intake:Issue('error', key, f'must be a list (got {type(items).__name__})')",
  "intake:Issue('error', str(p), 'observation.yaml is missing. Create a template with `bdbot intake init <folder>`.')",
  "metrics:raise ValueError(f'scope must be one of {SCOPES} (got: {scope!r})')",
  "nondim:raise KeyError(f\"dimensionless group '{name}' is not in the spec\")",
  "nondim:raise KeyError(f'{cat}.{symbol} is not in the spec ledger')",
  "params:raise ValueError('a parameter needs a name')",
  "params:raise ValueError(f\"{self.name!r} has no unit. Use '1' for a dimensionless quantity: blank must mean forgotten, not dimensionless.\")",
  "params:raise ValueError(f'category {category!r} already carries {sorted(self.params[category])}; it cannot also be N/A')",
  "params:raise ValueError(f'category {category!r} was declared not applicable ({self.absent[category]!r}); it cannot also carry {name!r}')",
  "params:raise ValueError(f'{self.name!r} has no provenance. Sketch, literature, handbook or estimate -- say which (rule 3).')",
  "params:raise ValueError(f'{self.name!r} has no value. If it is unknown, say so with Manifest.unknown() -- a null that looks like a number is how an invented value gets",
  "params:raise ValueError(f'{self.name!r}: tier must be one of {TIERS}, got {self.tier}')",
  "physical:raise KeyError(f\"{'.'.join(map(str, path))} not found ({self.path})\")",
  "runcard:raise FileNotFoundError(f'cannot seal {rundir.name}: missing {missing}. Both the prediction and the analysis plan are sealed together -- sealing the prediction ",
  "runcard:raise SealBroken(f'{Path(rundir).name}: ' + '; '.join(problems) + '. A broken seal is a hard stop: the comparison table is not built.')",
  "runcard:raise SealBroken(f'{Path(rundir).name}: no {SEAL_NAME}. Refusing to run without a sealed prediction. Pass require_seal=False only for a deliberately exploratory",
  "runcard:raise ValueError('refusing to write an empty seal -- it would pass on nothing')"
 ]
}
