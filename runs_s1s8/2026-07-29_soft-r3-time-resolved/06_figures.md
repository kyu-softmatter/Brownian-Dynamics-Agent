# S6 FIGURES

그림 6장 · 건너뜀 2건

> 그림 안 텍스트는 영문이다 — matplotlib 기본 폰트에 한글 글리프가 없다.
> 모든 시간·길이 축은 무차원 값과 물리 단위를 이중 표기한다 (§S6-4).

## 01_structure_timeseries.png

**무엇을 보이려는 그림인가** 구조가 언제 만들어지는가 — 최종값이 아니라 도달 시각과 표류 방향. 평탄해지지 않은 곡선은 이 런 길이로 평형화를 주장할 수 없다는 증거다

![위에서: 전역 `ψ₆` · Voronoi 결함 분율 · 배위수 종류 수 · 5-7 불균형 · 입자당 에너지. 띠는 **시드 앙상블 SE** (프레임 간 산포가…](figs/01_structure_timeseries.png)

위에서: 전역 `ψ₆` · Voronoi 결함 분율 · 배위수 종류 수 · 5-7 불균형 · 입자당 에너지. 띠는 **시드 앙상블 SE** (프레임 간 산포가 아니다 — 그것은 시간 상관을 포함한다). 마지막 프레임의 `ψ₆`: `A=0.1` → `0.055` · `A=1` → `0.059` · `A=10` → `0.156`. 보조축은 실제 시간이며 `τ_d = 38.2 분` 이다. **5-7 불균형이 0 으로 내려가면 결함이 전위 쌍으로 정리된 것이고**, 배위수 종류 수가 6→3 으로 줄면 액체형 결함이 사라진 것이다.

## 02_rdf_evolution.png

**무엇을 보이려는 그림인가** g(r) 의 첫 봉이 언제 자라고 언제 멈추는가 (그리고 멈추는가)

![시간창별 `g(r)`. 창이 진해질수록 늦은 시각이다 (`τ_d = 38.2 분`). 창끼리 겹치면 그 구간에서 구조가 더 변하지 않는 것이고, 계속 갈라지면…](figs/02_rdf_evolution.png)

시간창별 `g(r)`. 창이 진해질수록 늦은 시각이다 (`τ_d = 38.2 분`). 창끼리 겹치면 그 구간에서 구조가 더 변하지 않는 것이고, 계속 갈라지면 아직 진행 중이다. 붉은 파선은 `r = σ = 0.333 d` — 기준 원판이 접하는 거리다. **이 안쪽에 `g(r) > 0` 이면 5 µm 원판이 겹친다** (`A/r³` 에는 경질 코어가 없으므로 모델은 허용한다).

## 03_voronoi_A0.1.png

**무엇을 보이려는 그림인가** 결함의 **성격** — 분율 숫자로는 전위와 액체가 구별되지 않는다. 이 그림은 성격을 보이는 것이고, 시간 변화의 근거는 시계열 그림이다

![`A = 0.1` 의 Voronoi 타일링 시간열 (**시드 1개**). 색은 배위수 `z` — **빨강 `z=5`, 파랑 `z=7`, 회색 `z=6`**. 상…](figs/03_voronoi_A0.1.png)

`A = 0.1` 의 Voronoi 타일링 시간열 (**시드 1개**). 색은 배위수 `z` — **빨강 `z=5`, 파랑 `z=7`, 회색 `z=6`**. 상자는 `150 µm` 정사각, 주기경계다 (셀이 상자 밖으로 나가는 것은 주기 이웃 쪽으로 열린 셀이며 잘라내지 않았다 — 자르면 면적이 틀린다). 빨강·파랑이 **붙어서 쌍으로** 보이면 전위이고, 여러 색이 흩어져 있으면 액체다. ⚠ **패널의 결함 수 차이를 시간 변화로 읽지 말 것** — 이 계의 프레임별 결함 수는 정상상태에서 `64 ± 5` 로 요동한다 (프레임 표준편차). 패널 간 차이는 대부분 그 요동이다. 시간 변화는 시계열 그림에서 앙상블 평균으로 판단해야 한다.

## 04_voronoi_A1.png

**무엇을 보이려는 그림인가** 결함의 **성격** — 분율 숫자로는 전위와 액체가 구별되지 않는다. 이 그림은 성격을 보이는 것이고, 시간 변화의 근거는 시계열 그림이다

![`A = 1` 의 Voronoi 타일링 시간열 (**시드 1개**). 색은 배위수 `z` — **빨강 `z=5`, 파랑 `z=7`, 회색 `z=6`**. 상자는…](figs/04_voronoi_A1.png)

`A = 1` 의 Voronoi 타일링 시간열 (**시드 1개**). 색은 배위수 `z` — **빨강 `z=5`, 파랑 `z=7`, 회색 `z=6`**. 상자는 `150 µm` 정사각, 주기경계다 (셀이 상자 밖으로 나가는 것은 주기 이웃 쪽으로 열린 셀이며 잘라내지 않았다 — 자르면 면적이 틀린다). 빨강·파랑이 **붙어서 쌍으로** 보이면 전위이고, 여러 색이 흩어져 있으면 액체다. ⚠ **패널의 결함 수 차이를 시간 변화로 읽지 말 것** — 이 계의 프레임별 결함 수는 정상상태에서 `55 ± 5` 로 요동한다 (프레임 표준편차). 패널 간 차이는 대부분 그 요동이다. 시간 변화는 시계열 그림에서 앙상블 평균으로 판단해야 한다.

## 05_voronoi_A10.png

**무엇을 보이려는 그림인가** 결함의 **성격** — 분율 숫자로는 전위와 액체가 구별되지 않는다. 이 그림은 성격을 보이는 것이고, 시간 변화의 근거는 시계열 그림이다

![`A = 10` 의 Voronoi 타일링 시간열 (**시드 1개**). 색은 배위수 `z` — **빨강 `z=5`, 파랑 `z=7`, 회색 `z=6`**. 상자…](figs/05_voronoi_A10.png)

`A = 10` 의 Voronoi 타일링 시간열 (**시드 1개**). 색은 배위수 `z` — **빨강 `z=5`, 파랑 `z=7`, 회색 `z=6`**. 상자는 `150 µm` 정사각, 주기경계다 (셀이 상자 밖으로 나가는 것은 주기 이웃 쪽으로 열린 셀이며 잘라내지 않았다 — 자르면 면적이 틀린다). 빨강·파랑이 **붙어서 쌍으로** 보이면 전위이고, 여러 색이 흩어져 있으면 액체다. ⚠ **패널의 결함 수 차이를 시간 변화로 읽지 말 것** — 이 계의 프레임별 결함 수는 정상상태에서 `29 ± 7` 로 요동한다 (프레임 표준편차). 패널 간 차이는 대부분 그 요동이다. 시간 변화는 시계열 그림에서 앙상블 평균으로 판단해야 한다.

## 06_early_transient.png

**무엇을 보이려는 그림인가** 초기배치에서 정상상태로 가는 완화가 실제로 존재하는가, 그리고 존재하면 그 시간이 얼마인가 — 굵은 표집(0.2 τ_d)으로는 첫 프레임에 이미 끝나 보인다

![초기 과도구간을 **로그 시간축**으로. 별표는 초기배치(`t = 0`, 기각표집으로 `min_sep = 0.8 d` 를 강제한 배치) 이며 **세 `A` 의…](figs/06_early_transient.png)

초기 과도구간을 **로그 시간축**으로. 별표는 초기배치(`t = 0`, 기각표집으로 `min_sep = 0.8 d` 를 강제한 배치) 이며 **세 `A` 의 별표는 정확히 겹친다** — 초기배치가 `A` 에 의존하지 않으므로 (같은 시드 → 같은 배치) 세 곡선은 **같은 점에서 갈라진다.** 곡선은 시드 앙상블 평균 ± SE 다. 파선은 단일지수 적합이며 **완화 폭이 잡음의 2배를 넘지 못하면 그리지 않는다** — 정상상태 요동에 지수를 맞추면 항상 어떤 `τ` 가 나오기 때문이다. `A=0.1`: `τ = 0.0311 ± 0.00279 τ_d` (`71 s`), 폭 `-0.145` · `A=1`: `τ = 0.042 ± 0.01 τ_d` (`96 s`), 폭 `-0.050` · `A=10`: `τ = 0.0982 ± 0.00634 τ_d` (`225 s`), 폭 `+0.174`

---

## 그리지 않은 그림과 이유

| 그림 | 이유 |
|---|---|
| `msd` | 이 카드의 목적동역학은 평형 구조다. 수송량은 묻지 않았다 (카드 §2 관측량 목록에 MSD 가 없다) |
| `snapshots_3d` | 2D 계다 — 3D 레이트레이싱(fresnel)은 설치하지 않았다 |

> 이유 없이 건너뛴 그림은 없다 — `FigureSet.skip()` 이 이유를 필수로 요구한다.
