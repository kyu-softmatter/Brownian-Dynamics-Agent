# REPORT — `2026-07-28_cli-e2e-test`

**판정 (제안)** `PASS_WITH_INCONCLUSIVE` · **확정 대기** (`confirmed_by: null`)

**질문** 이 광집게에 갇힌 반지름 5 um 입자의 열요동을 정량화하고 해석해와 대조하라. 부수 검증: 이 계에서 tau_D 기준 무차원화가 실제로 실패하는가.
**카드** [`passive-sphere--harmonic-trap`](../../knowledge/wiki/systems/passive-sphere--harmonic-trap.md)

**총 계산시간 `2.3 s`** · 4런 · 런당 평균 `0.58 s`

**봉인 검증** ✅ 봉인 검증 통과 — 2개 문서 실행 후 미변경

검증 명령 (이 코드 없이도 확인된다):

```bash
shasum -a 256 -c SEALED.sha256
```

---

## 1. 판정 요약

| 양 | 예측 (봉인) | 측정 | tolerance | 편차 | 검정력 | verdict |
|---|---|---|---|---|---|---|
| `var_x_star` | `1.00251` | `1.00577` ± `0.00446` | `±1%` | +0.33% (0.73σ) | `0.56σ` | ⚠ **INCONCLUSIVE** |
| `var_x_nm2` | `414.195` | `416.583` ± `1.85` | `±1.5%` | +0.58% (1.29σ) | — | **PASS** |
| `var_r_nm2` | `828.389` | `833.165` ± `3.7` | `±1.5%` | +0.58% (1.29σ) | — | **PASS** |
| `msd_plateau_star` | `4.01003` | `4.02149` ± `0.00607` | `±2%` | +0.29% (1.89σ) | — | **PASS** |
| `tau_star` | `1` | `0.999038` ± `0.00372` | `±5%` | -0.10% (0.26σ) | — | **PASS** |
| `tau_trap_ms` | `8.06441` | `8.05665` ± `0.03` | `±5%` | -0.10% (0.26σ) | — | **PASS** |
| `msd_r_squared` | `>0.99` | `0.999977` ± `7.79e-06` | `>0.99` | +0.00998 (1280.34σ) | — | **PASS** |
| `kT_conf_star` | `1` | `1.00577` ± `0.00446` | `±2%` | +0.58% (1.29σ) | — | **PASS** |
| `plateau_over_2d_var` | `1` | `0.99961` ± `0.00151` | `±1%` | -0.04% (0.26σ) | — | **PASS** |

**9개 중 8 PASS · 1 INCONCLUSIVE · 0 FAIL.**

### PASS 아닌 항목
- **`var_x_star`** — INCONCLUSIVE: 설계 검정력 0.56σ < 1σ — 경쟁 가설(1)과 구별되지 않는다. 3σ 판별에 표본 28.5배 필요. ★ 예견된 한계이지 실패가 아니다

> `INCONCLUSIVE` 는 실패가 아니다 — 통계오차가 tolerance 보다 커서 **판정이 불가능**하다는 사실이다. 필요한 표본 배수가 위에 있다.

### 부기 (PASS 항목 포함)
- **`msd_plateau_star`**: plateau 와 <x*^2> 는 독립 경로(시계열 vs 스냅샷)이지만 **같은 편향을 공유한다**. 경로 독립성은 plateau_over_2d_var 이 따로 검사한다
- **`kT_conf_star`**: 순수 조화 트랩에서는 <x^2> 검사와 대수적으로 동일 — 독립 검사가 아니다

> 예측 문서에 적힌 한계다. PASS 라고 사라지지 않는다 — 여기 '이건 독립 검사가 아니다' 류가 들어 있고, 그게 빠지면 결론이 과대해진다.

```yaml
verdict_overall: PASS_WITH_INCONCLUSIVE
proposed_by: agent
confirmed_by: null            # ← 사람 확정 대기
pass: 8
inconclusive: 1
fail: 0
```

> 판정은 **제안**이다. 사람이 `confirmed_by` 를 채우기 전까지 벤치마크 원장 집계에 들어가지 않는다 (CLAUDE.md §판정).

---

## 2. 시스템 명세와 게이트 (S3)

| 검사 | 상태 | 값 | 문턱 | 비고 |
|---|---|---|---|---|
| `overdamped` | ✅ pass | `0.0008454` | `0.01` | τ_i/τ_process ≪ 1 — BD 전제 유효 |
| `stokes_reynolds` | ✅ pass | `1.47e-05` | `0.01` | Re ≪ 1 — Stokes 항력 유효 |
| `equipartition` | ⏳ S7 판정 |  |  | 이 카드의 1급 게이트 |
| `em_bias_reproduced` | ⏳ S7 판정 |  |  | <x*^2> = 1/(1-dt*/2) |
| `configurational_temperature` | ⏳ S7 판정 |  |  | 카드 선언: required |
| `dt_over_tau_trap` | ⏳ S7 판정 |  |  | dt* = 5e-3 <= 1e-2 |
| `box_much_larger_than_l_trap` | ✅ pass | `200` | `10` | 박스가 기준길이의 200배 |
| `equilibration_detection` | ⏳ S7 판정 |  |  | 열평형에 도달한다 |
| `step_displacement_vs_sigma` | — off |  |  | 쌍 상호작용이 없어 겹침이 발생할 수 없다 |
| `r_cut_le_half_box` | — off |  |  | 쌍 포텐셜 없음 |
| `polydispersity` | — off |  |  | 상호작용 없어 결정화 불가 |
| `hydrodynamics_neglected` | ⏳ S7 판정 |  |  | 단일 입자·벽 없음 → 무한매질 Stokes 가 정확 |
| `packing_fraction` | — off | `4741` | `0.9` | 면적분율은 의미 없음 — 쌍 상호작용이 없어 배제부피가 없다 (입자는 독립 복제) |

⏳ **S7 이 판정해야 하는 게이트 6개** — S3 에서는 계산할 수 없는 양이다: `equipartition`, `em_bias_reproduced`, `configurational_temperature`, `dt_over_tau_trap`, `equilibration_detection`, `hydrodynamics_neglected`

---

## 3. 무차원화 (S4)

기준 척도: **scales_harmonic_trap: (l_trap, kT, tau_trap)**

| 물리량 | SI | 무차원 | 역변환 계수 |
|---|---|---|---|
| 길이 척도 | `2.03518e-08` m | `1` | `2.03518e-08` |
| 에너지 척도 `kT` | `4.14195e-21` J | `1` | `4.14195e-21` |
| 시간 척도 | `0.00806441` s | `1` | `0.00806441` |
| `σ` | `1e-05` m | `491.358` | `2.03518e-08` |
| `γ` | `8.06441e-08` kg/s | `1` | `8.06441e-08` |
| `D₀` | `5.13608e-14` m²/s | `1` | `5.13608e-14` |
| `k` | `1e-05` N/m | `1` | `1e-05` |
| `Δt` | `4.0322e-05` s | `0.005` | `0.00806441` |
| **척도 출처** | scales_harmonic_trap: (l_trap, kT, tau_trap) | | |

**왕복 오차** 최대 `1.64e-16` (게이트 `< 1e-12`) — ✅ 통과

`Δt*` = `0.005` · 지배 제약 **spec(명시값)**

기록용 (게이트 아님 — 다른 논문과 비교할 때만 쓴다):
- `dt_over_tau_D` = `2.071e-08`

| 무차원수 | 값 |
|---|---|
| `sigma_over_ref_length` | `491.358` |
| `tau_D_over_ref_time` | `241432` |
| `k_star_sigma` | `241432` |
| `k_star` | `1` |
| `l_trap_over_sigma` | `0.00203518` |
| `tau_D_over_tau_trap` | `241432` |
| `tau_inertial_over_ref_time` | `0.000845363` |
| `reynolds` | `1.46952e-05` |

---

## 4. 그림 (S6)

### 01_msd.png

![조화 트랩 MSD (2D, 시드 4개). 단시간에 자유확산 기울기 1 을 따르고 `t ≳ τ_trap` 에서 `2d = 4` 로 포화한다. 위/오른쪽 축은 물리…](figs/01_msd.png)

조화 트랩 MSD (2D, 시드 4개). 단시간에 자유확산 기울기 1 을 따르고 `t ≳ τ_trap` 에서 `2d = 4` 로 포화한다. 위/오른쪽 축은 물리 단위 (`τ_trap`·`ℓ_trap²` 환산).

### 03_distribution.png

![평형 위치 분포 (측정 표준편차로 규격화 — **폭이 아니라 형태**를 본다. 폭은 `⟨x²⟩` 가 따로 검사한다). 첨도 `2.9976 ± 0.0245`, 예…](figs/03_distribution.png)

평형 위치 분포 (측정 표준편차로 규격화 — **폭이 아니라 형태**를 본다. 폭은 `⟨x²⟩` 가 따로 검사한다). 첨도 `2.9976 ± 0.0245`, 예측 `3 − 1.2 dt* = 2.9940` → `0.15σ`. ★ **정확히 3.000 이면 오히려 이상하다** — HOOMD 노이즈가 균일분포이므로 `dt*` 차수의 잔여 비가우시안성이 남는다. ⚠ `2 τ_trap` 이상 떨어진 **독립 프레임 20개**만 썼다 — 상관 프레임을 쓰면 표본 수가 부풀어 오차막대가 거짓으로 작아진다.

### 04_stationarity.png

![**위** 독립 표본 시점별 `⟨x*²⟩`(색)과 배위 온도 `kT_conf`(검정 ×). 추세 없이 요동하면 평형에 도달했다는 뜻이다. EM 편향 `0.251…](figs/04_stationarity.png)

**위** 독립 표본 시점별 `⟨x*²⟩`(색)과 배위 온도 `kT_conf`(검정 ×). 추세 없이 요동하면 평형에 도달했다는 뜻이다. EM 편향 `0.251%` 는 시점별 요동(`±5 %` 규모)보다 훨씬 작아 여기서는 보이지 않는다 — 그래서 시드 앙상블 평균이 필요하다.

**아래** 두 통계량의 상대 잔차. 최대 `4.4e-16` — 부동소수점 수준이다. ⚠ **순수 조화 트랩에서 `kT_conf` 는 `⟨x²⟩` 검사와 대수적으로 동일하다** (트랩 카드 §6 이 예고). 이 그림은 그것을 주장하지 않고 **보여준다** — 독립 검사가 되려면 쌍 상호작용이 필요하다.

### 05_displacements.png

![프레임 간 변위 분포 (`16` 스텝 간격). 최대값 `1.872 ℓ_trap` = `3.81e-03 σ` — 겹침 문턱 `0.1 σ` 에서 26 배 이하다.…](figs/05_displacements.png)

프레임 간 변위 분포 (`16` 스텝 간격). 최대값 `1.872 ℓ_trap` = `3.81e-03 σ` — 겹침 문턱 `0.1 σ` 에서 26 배 이하다. ⚠ **스텝당 변위가 아니다.** 균일 노이즈의 `max/σ = √3` 상한은 여기 적용되지 않는다 (여러 스텝의 합).

### 06_snapshots.png

![초기·중간·최종 스냅샷. 원은 `1, 2, 3 ℓ_trap` (`ℓ_trap = 20.35 nm`). 세 시점의 분포가 눈으로 구별되지 않으면 평형에 도달했다는…](figs/06_snapshots.png)

초기·중간·최종 스냅샷. 원은 `1, 2, 3 ℓ_trap` (`ℓ_trap = 20.35 nm`). 세 시점의 분포가 눈으로 구별되지 않으면 평형에 도달했다는 뜻이다. 입자 1000개는 같은 트랩 안의 **독립 복제**이지 서스펜션이 아니다.

---

## 5. 재현 가능성

| 항목 | 값 |
|---|---|
| run_id | `2026-07-28_cli-e2e-test` |
| spec 해시 | `cb5e6ce29cd6f2cfc5a82ea1c4ba564a3e229c0f4263b013dfd56f2a44096c16` |
| 코드 해시 | `ad9a21d4efe3` |
| git 커밋 | `ad82bdb` |
| env 해시 | `eaf847c12616` |
| 시드 | `[1, 2, 3, 4]` |
| hoomd | `7.1.0` |
| numpy | `2.5.1` |
| scipy | `1.18.0` |
| python | `3.12.13` |
| **git 상태** | ⚠️ **커밋되지 않은 변경 있음** — `git_rev` 만으로 이 런이 재현되지 않는다 |

---

## 6. 에이전트가 쓴 문서

- 판독 (S1) _`01_intake.md` 가 없다 — 판독은 에이전트가 써야 한다._
- 예측 (S2, 봉인) → [`02_prediction.md`](02_prediction.md)
- 검증 서술 (S7) _`07_validation.md` 가 없다 — 검증 서술은 에이전트가 써야 한다._
- 결론 (S8) _`08_conclusion.md` 가 없다 — 결론은 에이전트가 써야 한다._

> `simbot` 은 수치를 만들고, 질문에 대한 답·원인 가설·다음 실험 제안은 에이전트가 쓴다. 이 리포트는 그 문서를 **인용**하며 대신 쓰지 않는다.
