# S6 FIGURES

그림 1장 · 건너뜀 1건

> 그림 안 텍스트는 영문이다 — matplotlib 기본 폰트에 한글 글리프가 없다.
> 모든 시간·길이 축은 무차원 값과 물리 단위를 이중 표기한다 (§S6-4).

## 01_seed_convergence.png

**무엇을 보이려는 그림인가** 저시드 예비런이 왜 낙관적인 검정력을 주는가 — 편향된 차이와 과소추정된 오차가 곱해진다. 그리고 최종 표본에서 τ 가 갈리는가

![**① `τ` 추정치가 시드 수와 함께 움직인다.** 이 곡선들은 **같은 궤적의 중첩 부분집합**에서 나왔으므로 기울기는 데이터 차이가 아니라 **추정량의 `…](figs/01_seed_convergence.png)

**① `τ` 추정치가 시드 수와 함께 움직인다.** 이 곡선들은 **같은 궤적의 중첩 부분집합**에서 나왔으므로 기울기는 데이터 차이가 아니라 **추정량의 `k` 의존성**이다 — 즉 저시드 편향이다. 오차막대는 시드 부트스트랩 (`curve_fit` 공분산이 아니다). **② 사전등록 예상(별)과 실제(선)의 어긋남.** **③ 최종 `k = 1513` 의 결함 완화 곡선과 단일지수 적합**; 별표는 초기배치(`t=0`)이며 두 `A` 가 같은 점에서 갈라진다. `τ_d = 38.2 분`.

---

## 그리지 않은 그림과 이유

| 그림 | 이유 |
|---|---|
| `voronoi` | 이 런은 완화시간만 본다 — 배치의 성격은 부모 런이 이미 냈다 |

> 이유 없이 건너뛴 그림은 없다 — `FigureSet.skip()` 이 이유를 필수로 요구한다.
